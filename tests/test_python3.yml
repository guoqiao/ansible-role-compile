#!/usr/bin/env ansible-playbook
---
- hosts: localhost
  vars:
    # have to use system python3
    ansible_python_interpreter: /usr/bin/python3
  tasks:

    - name: build python3
      include_role:
        name: guoqiao.xbuild
        vars_from: python3
